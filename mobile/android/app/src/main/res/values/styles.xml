<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
      The app theme.

      `windowBackground` is the important line, and it is not cosmetic.

      Capacitor 8 decides for itself whether the WebView draws under the system
      bars: its `SystemBars` handler passes the insets through to CSS only on
      **WebView 140 and newer**, and on anything older it pads the WebView's
      parent instead, so the page is laid out between the bars. On that older
      path the strip behind the status bar is painted by the *window*, not by
      the app — and with `Theme.AppCompat.DayNight` on a phone in light mode
      that strip came out **white**, a bright band across the top of an
      otherwise black app. Measured on an emulator running WebView 133.

      `#0a0a0f` is the `bg-base` token from the renderer, so the band matches
      the app whichever path the device takes. `forceDarkAllowed=false` stops
      Android's automatic dark-mode inversion touching it.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:colorBackground">@color/splash_background</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>


    <!--
      The launch theme, using the Android 12 splash screen API rather than a
      full-bleed bitmap.

      Capacitor's template points `android:background` at `@drawable/splash`,
      which stretches one PNG across whatever aspect ratio the device has — on a
      tall phone that means a cropped, off-centre logo, and it is the single
      most recognisable "this is a wrapped web app" tell. The platform API
      instead centres a masked icon on a solid colour at every size, and hands
      over to the WebView without a flash, because the colour is the same one
      the app paints its own background with.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>